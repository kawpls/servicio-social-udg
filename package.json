{
  "name": "ayudaparalamujer",
  "description": "",
  "scripts": {
    "tailwind": "npx tailwindcss -i ./src/tailwind.css -o ./_site/styles.css --watch",
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && npx tailwindcss -i ./src/tailwind.css -o ./_site/styles.css --minify"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@tailwindcss/typography": "^0.5.9",
    "daisyui": "^2.51.3",
    "tailwindcss": "^3.2.7"
  }
}